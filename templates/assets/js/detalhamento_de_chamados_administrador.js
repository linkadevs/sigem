const botoes_menu = document.querySelectorAll('aside button');

botoes_menu.forEach(botao => {
    botao.addEventListener('click', () => {
        // Lógica de redirecionamento futuro
        // window.location.href = '';
    });
});